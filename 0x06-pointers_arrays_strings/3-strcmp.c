#include "main.h"

/**
 * _strcmp -> a function that  compares two strings.
 * @s1: string parameter
 * @s2: string parameter
 * 
 * Return: int
 */

int _strcmp(char *s1, char *s2)
{
	int n;
	n = strcmp(s1, s2);
	return (n);

}
