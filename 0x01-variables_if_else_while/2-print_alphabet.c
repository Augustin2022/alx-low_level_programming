#include <stdlib.h>
#include <stdio.h>

/**
 * main - print alphabet
 *
 * Description: Print alphabet in low case followd
 * by a new line
 * Return: return always 0
 */
int main(void)
{
	char x;
	for(x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
		putchar("\n");
	}
	return (0):
}
